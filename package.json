{
  "name": "onebe-corporate",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs && node scripts/serve.mjs",
    "preview": "node scripts/serve.mjs",
    "check:aio:static": "node scripts/check-static.mjs",
    "check:aio:http": "node scripts/check-http.mjs",
    "check:aio:browser": "playwright test",
    "test": "npm run build && npm run check:aio:static && node --test tests/form.test.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "fast-xml-parser": "^5.11.1",
    "parse5": "^8.0.1"
  }
}
